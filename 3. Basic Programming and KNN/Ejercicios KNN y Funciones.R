#Ejercicio KNN y funciones

#1. En la carpeta de la Semana 3 se encuentra un dataset de nombre winequality-red.csv
  #Encuentren cómo importar los datos en R
  #El dataset contiene caracteristicas de vino tinto y si nivel de calidad
  #Una de la variables "quality" que es la variable de interes pero no es factor
  #La variable toma los valores 3,4,5,6,7,8
  #Deben crear una variable factor con tres categorias cat1 = 3,4, cat2 = 5,6, cat3 = 7,8
  #Con esto listo desarrollen todo el proceso para implementar KNN


#2. Escriban una funcion que tenga como argumento dos vectores
  #La funcion debe sumar las posiciones respectivas de cada vector y agregar el resultado a una lista nueva
  #Es decir, la funcion debe sumar la posicion 1 del vector1 y la posicion 1 del vector2 y este resultado agregarlo a una nueva lista
  #Esto para cada posicion del vector
  #La funcion debe resultar en una lista con los resultados

#3. Escriban una funcion que tenga como argumento dos vectores
  #La funcion debe sumar cada posicion del vector1 con todas las posiciones del vector2.
  #Es decir, la funcion debe sumar la posicion 1 del vector1 y todas las posiciones del vector2
  #Luego debe sumar la posicion 2 del vector1 con todas las posiciones del vector2 y asi sucesivamente
  #Cada resultado debe ser agregado a una lista nueva
  #La funcion debe resultar en una lista con los resultados
